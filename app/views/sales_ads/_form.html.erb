<%= form_for(@sales_ad) do |f| %>
  <% if @sales_ad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sales_ad.errors.count, "error") %> prohibited this sales_ad from being saved:</h2>

      <ul>
      <% @sales_ad.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= stepper f %>
<% end %>
